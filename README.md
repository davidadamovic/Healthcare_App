# Healthcare_App
This is an API with an app that takes user inputs and interacts with database. 
The Database has five tables: Hospital, Department, Patient, Treatment and many-to-many table Patient_Treatment.
An user gets to choose a table from which you can choose to see all data, add a column, delete a column, update a column and exit.

